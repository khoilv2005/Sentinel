# SentinelView owns alert evaluation in the Control API:
# telemetry -> Monitoring Rules -> Services -> Problems -> Notifications.
#
# Prometheus is the time-series/history backend and must not maintain a second
# set of independently configured alert thresholds. Keeping this file valid but
# empty prevents threshold drift between the first-party UI and Prometheus.
groups: []
