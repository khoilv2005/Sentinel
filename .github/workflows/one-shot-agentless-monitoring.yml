name: one-shot-agentless-monitoring

on:
  push:
    branches: [feature/agentless-monitoring-ci]
    paths:
      - '.github/workflows/one-shot-agentless-monitoring.yml'

permissions:
  contents: write

jobs:
  build-feature:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/agentless-monitoring-ci
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Patch backend, UI and container stack
        shell: python
        run: |
          from pathlib import Path
          import textwrap

          def read(path):
              return Path(path).read_text(encoding='utf-8')

          def write(path, text):
              Path(path).write_text(text, encoding='utf-8')

          def replace_once(path, old, new):
              text = read(path)
              if old not in text:
                  raise SystemExit(f'Expected patch anchor not found in {path}: {old[:120]!r}')
              write(path, text.replace(old, new, 1))

          # Database models: separate tables avoid ALTER TABLE requirements for existing v0.3.0 databases.
          models = read('services/control-api/app/models.py')
          if 'class CredentialProfile(Base):' not in models:
              models += textwrap.dedent('''


              class CredentialProfile(Base):
                  __tablename__ = "credential_profiles"
                  id: Mapped[str] = mapped_column(String(36), primary_key=True, default=uid)
                  name: Mapped[str] = mapped_column(String(160), unique=True, index=True)
                  credential_type: Mapped[str] = mapped_column(String(32), index=True)
                  username: Mapped[str | None] = mapped_column(String(255), nullable=True)
                  secret_encrypted: Mapped[str] = mapped_column(Text)
                  options_json: Mapped[dict] = mapped_column(JSON, default=dict)
                  enabled: Mapped[bool] = mapped_column(Boolean, default=True, index=True)
                  created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow)
                  updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow, onupdate=utcnow)


              class AgentlessMonitor(Base):
                  __tablename__ = "agentless_monitors"
                  id: Mapped[str] = mapped_column(String(36), primary_key=True, default=uid)
                  device_id: Mapped[str] = mapped_column(String(36), ForeignKey("devices.id", ondelete="CASCADE"), index=True)
                  method: Mapped[str] = mapped_column(String(32), index=True)
                  credential_id: Mapped[str] = mapped_column(String(36), ForeignKey("credential_profiles.id"), index=True)
                  interval_seconds: Mapped[int] = mapped_column(Integer, default=60)
                  enabled: Mapped[bool] = mapped_column(Boolean, default=True, index=True)
                  last_poll_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True, index=True)
                  last_success_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True, index=True)
                  last_error: Mapped[str | None] = mapped_column(Text, nullable=True)
                  consecutive_failures: Mapped[int] = mapped_column(Integer, default=0)
                  created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow)
                  updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow, onupdate=utcnow)


              class AgentlessTelemetryLatest(Base):
                  __tablename__ = "agentless_telemetry_latest"
                  device_id: Mapped[str] = mapped_column(String(36), ForeignKey("devices.id", ondelete="CASCADE"), primary_key=True)
                  source: Mapped[str] = mapped_column(String(32), index=True)
                  collected_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), index=True, default=utcnow)
                  cpu_usage_percent: Mapped[float | None] = mapped_column(Float, nullable=True)
                  memory_total_bytes: Mapped[int | None] = mapped_column(BigInteger, nullable=True)
                  memory_used_bytes: Mapped[int | None] = mapped_column(BigInteger, nullable=True)
                  memory_usage_percent: Mapped[float | None] = mapped_column(Float, nullable=True)
                  uptime_seconds: Mapped[int | None] = mapped_column(BigInteger, nullable=True)
                  process_count: Mapped[int | None] = mapped_column(Integer, nullable=True)
                  disks: Mapped[list] = mapped_column(JSON, default=list)
                  interfaces: Mapped[list] = mapped_column(JSON, default=list)
                  raw: Mapped[dict] = mapped_column(JSON, default=dict)
                  updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow, onupdate=utcnow)
              ''')
              write('services/control-api/app/models.py', models)

          config = read('services/control-api/app/config.py')
          if 'credential_secret =' not in config:
              config = config.replace(
                  '    session_hours = int(os.getenv("SENTINEL_SESSION_HOURS", "12"))',
                  '    session_hours = int(os.getenv("SENTINEL_SESSION_HOURS", "12"))\n'
                  '    credential_secret = os.getenv("SENTINEL_CREDENTIAL_SECRET", os.getenv("SENTINEL_SESSION_SECRET", "change-me-session-secret"))\n'
                  '    agentless_workers = int(os.getenv("SENTINEL_AGENTLESS_WORKERS", "24"))'
              )
              write('services/control-api/app/config.py', config)

          req = read('services/control-api/requirements.txt')
          for dep in ['cryptography>=44,<48', 'paramiko>=3.5,<5', 'pywinrm>=0.5,<1']:
              if dep not in req:
                  req += dep + '\n'
          write('services/control-api/requirements.txt', req)

          dockerfile = read('services/control-api/Dockerfile')
          dockerfile = dockerfile.replace('iputils-ping iproute2 \\', 'iputils-ping iproute2 snmp \\')
          write('services/control-api/Dockerfile', dockerfile)

          # Register API router and expose agentless telemetry through the existing Prometheus endpoint.
          main = read('services/control-api/app/main.py')
          if 'AgentlessTelemetryLatest,' not in main:
              main = main.replace('    AgentTelemetryLatest,\n', '    AgentTelemetryLatest,\n    AgentlessTelemetryLatest,\n')
          if 'from .agentless_api import router as agentless_router' not in main:
              main = main.replace(
                  'from .monitoring import all_services, is_agent_online, services_for_device, sync_problems',
                  'from .monitoring import all_services, is_agent_online, latest_telemetry_for_device, services_for_device, sync_problems\nfrom .agentless_api import router as agentless_router'
              )
          if 'app.include_router(agentless_router)' not in main:
              main = main.replace('API_REQUESTS = Counter(', 'app.include_router(agentless_router)\n\nAPI_REQUESTS = Counter(', 1)
          old_host_t = '    telemetry = db.get(AgentTelemetryLatest, agent.id) if agent else None\n    problems = list(db.execute(select(Problem)'
          if old_host_t in main:
              main = main.replace(old_host_t, '    telemetry, _telemetry_source = latest_telemetry_for_device(db, device)\n    problems = list(db.execute(select(Problem)', 1)
          if '"source": getattr(row, "source", "agent"),' not in main:
              main = main.replace('    return {\n        "collected_at": row.collected_at,', '    return {\n        "source": getattr(row, "source", "agent"),\n        "collected_at": row.collected_at,', 1)
          marker = '            NET_TX.labels(*nlabels).set(float(nic.get("transmit_bytes_total", 0) or 0))\n\n\n@app.get("/metrics")'
          if 'managed_device_ids = {a.device_id for a in agents}' not in main:
              extra = '''            NET_TX.labels(*nlabels).set(float(nic.get("transmit_bytes_total", 0) or 0))

    managed_device_ids = {a.device_id for a in agents}
    for t in db.execute(select(AgentlessTelemetryLatest)).scalars():
        if t.device_id in managed_device_ids:
            continue
        d = db.get(Device, t.device_id)
        if d is None:
            continue
        labels = (d.id, d.hostname or d.ip_address, d.site or "default")
        if t.cpu_usage_percent is not None: CPU_USAGE.labels(*labels).set(t.cpu_usage_percent)
        if t.memory_total_bytes is not None: MEM_TOTAL.labels(*labels).set(t.memory_total_bytes)
        if t.memory_used_bytes is not None: MEM_USED.labels(*labels).set(t.memory_used_bytes)
        if t.memory_usage_percent is not None: MEM_USAGE.labels(*labels).set(t.memory_usage_percent)
        if t.uptime_seconds is not None: UPTIME.labels(*labels).set(t.uptime_seconds)
        if t.process_count is not None: PROCESS_COUNT.labels(*labels).set(t.process_count)
        for disk in t.disks or []:
            mount = str(disk.get("mountpoint", "unknown")); fstype = str(disk.get("fstype", ""))
            dlabels = (*labels, mount, fstype)
            DISK_TOTAL.labels(*dlabels).set(float(disk.get("total_bytes", 0) or 0))
            DISK_USED.labels(*dlabels).set(float(disk.get("used_bytes", 0) or 0))
            DISK_USAGE.labels(*dlabels).set(float(disk.get("usage_percent", 0) or 0))
        for nic in t.interfaces or []:
            name = str(nic.get("name", "unknown")); nlabels = (*labels, name)
            NET_RX.labels(*nlabels).set(float(nic.get("receive_bytes_total", 0) or 0))
            NET_TX.labels(*nlabels).set(float(nic.get("transmit_bytes_total", 0) or 0))


@app.get("/metrics")'''
              if marker not in main:
                  raise SystemExit('Prometheus agent loop anchor not found')
              main = main.replace(marker, extra, 1)
          write('services/control-api/app/main.py', main)

          monitoring = read('services/control-api/app/monitoring.py')
          monitoring = monitoring.replace(
              'from .models import AgentTelemetryLatest, AlertRule, Device, ManagedAgent, Problem',
              'from .models import AgentlessMonitor, AgentlessTelemetryLatest, AgentTelemetryLatest, AlertRule, Device, ManagedAgent, Problem'
          )
          if 'def latest_telemetry_for_device(' not in monitoring:
              anchor = '\ndef services_for_device(db: Session, device: Device) -> list[dict]:\n'
              helper = '''
def latest_telemetry_for_device(db: Session, device: Device):
    agent = db.execute(select(ManagedAgent).where(ManagedAgent.device_id == device.id)).scalar_one_or_none()
    if agent is not None:
        row = db.get(AgentTelemetryLatest, agent.id)
        if row is not None:
            return row, "agent"
    row = db.get(AgentlessTelemetryLatest, device.id)
    if row is not None:
        return row, row.source
    return None, None


def services_for_device(db: Session, device: Device) -> list[dict]:
'''
              if anchor not in monitoring:
                  raise SystemExit('monitoring services anchor not found')
              monitoring = monitoring.replace(anchor, helper, 1)
          monitoring = monitoring.replace(
              '    telemetry = db.get(AgentTelemetryLatest, agent.id) if agent else None',
              '    telemetry, telemetry_source = latest_telemetry_for_device(db, device)'
          )
          if 'service_key": f"agentless:{monitor.method}"' not in monitoring:
              anchor = '    if telemetry:\n        metrics: Iterable[tuple[str, str, float | int | None, str | None]] = ('
              agentless_health = '''    monitor = db.execute(
        select(AgentlessMonitor)
        .where(AgentlessMonitor.device_id == device.id, AgentlessMonitor.enabled.is_(True))
        .order_by(AgentlessMonitor.updated_at.desc())
    ).scalars().first()
    if monitor is not None:
        now = utcnow()
        success = monitor.last_success_at
        if success is not None and success.tzinfo is None:
            success = success.replace(tzinfo=timezone.utc)
        stale = success is None or (now - success).total_seconds() > max(120, monitor.interval_seconds * 3)
        monitor_state = "critical" if stale and monitor.consecutive_failures >= 3 else "unknown" if success is None else "ok"
        services.append({
            "host_id": device.id,
            "hostname": hostname,
            "service_key": f"agentless:{monitor.method}",
            "service_name": f"{monitor.method.upper()} monitoring",
            "state": monitor_state,
            "value": "connected" if monitor_state == "ok" else "unavailable" if monitor_state == "critical" else "pending",
            "unit": None,
            "message": monitor.last_error or f"{monitor.method.upper()} collector is healthy",
            "updated_at": monitor.last_success_at or monitor.last_poll_at,
        })

    if telemetry:
        metrics: Iterable[tuple[str, str, float | int | None, str | None]] = ('''
              if anchor not in monitoring:
                  raise SystemExit('telemetry service anchor not found')
              monitoring = monitoring.replace(anchor, agentless_health, 1)
          write('services/control-api/app/monitoring.py', monitoring)

          compose = read('docker-compose.yml')
          if 'SENTINEL_CREDENTIAL_SECRET:' not in compose:
              compose = compose.replace(
                  '      SENTINEL_SESSION_SECRET: ${SENTINEL_SESSION_SECRET:-change-me-session-secret}\n',
                  '      SENTINEL_SESSION_SECRET: ${SENTINEL_SESSION_SECRET:-change-me-session-secret}\n      SENTINEL_CREDENTIAL_SECRET: ${SENTINEL_CREDENTIAL_SECRET:-change-me-credential-secret}\n'
              )
          if '  agentless-worker:' not in compose:
              worker = '''  agentless-worker:
    image: sentinelview-control-api:0.3.0
    build:
      context: .
      dockerfile: services/control-api/Dockerfile
    command: ["python", "-m", "app.agentless_worker"]
    environment:
      SENTINEL_DB_URL: ${SENTINEL_DB_URL:-postgresql+psycopg://sentinel:sentinel@postgres:5432/sentinel}
      SENTINEL_LOG_LEVEL: ${SENTINEL_LOG_LEVEL:-INFO}
      SENTINEL_CREDENTIAL_SECRET: ${SENTINEL_CREDENTIAL_SECRET:-change-me-credential-secret}
      SENTINEL_AGENTLESS_WORKERS: ${SENTINEL_AGENTLESS_WORKERS:-24}
    depends_on:
      postgres:
        condition: service_healthy
      control-api:
        condition: service_healthy
    restart: unless-stopped

'''
              compose = compose.replace('  web-ui:\n', worker + '  web-ui:\n', 1)
          if '  discovery-worker:\n    image: sentinelview-control-api:0.3.0\n    build:' not in compose:
              compose = compose.replace(
                  '  discovery-worker:\n    image: sentinelview-control-api:0.3.0\n',
                  '  discovery-worker:\n    image: sentinelview-control-api:0.3.0\n    build:\n      context: .\n      dockerfile: services/control-api/Dockerfile\n'
              )
          write('docker-compose.yml', compose)

          index = read('services/web-ui/index.html')
          if 'data-route="agentless"' not in index:
              index = index.replace(
                  '<a href="#agents" class="nav-item" data-route="agents"><span class="nav-icon">◎</span><span>Agents</span></a>',
                  '<a href="#agents" class="nav-item" data-route="agents"><span class="nav-icon">◎</span><span>Agents</span></a>\n          <a href="#agentless" class="nav-item" data-route="agentless"><span class="nav-icon">⌁</span><span>Agentless</span></a>'
              )
          if 'data-route="credentials"' not in index:
              index = index.replace(
                  '<a href="#rules" class="nav-item" data-route="rules"><span class="nav-icon">⟐</span><span>Monitoring Rules</span></a>',
                  '<a href="#credentials" class="nav-item" data-route="credentials"><span class="nav-icon">⌾</span><span>Credentials</span></a>\n          <a href="#rules" class="nav-item" data-route="rules"><span class="nav-icon">⟐</span><span>Monitoring Rules</span></a>'
              )
          write('services/web-ui/index.html', index)

          app = read('services/web-ui/js/app.js')
          if "agentless: ['Agentless Monitoring'" not in app:
              app = app.replace(
                  "  agents: ['Agents', 'Enroll and manage outbound telemetry agents'],",
                  "  agents: ['Agents', 'Enroll and manage outbound telemetry agents'],\n  agentless: ['Agentless Monitoring', 'Monitor Windows, Linux and SNMP devices without installing an agent'],"
              )
          if "credentials: ['Credentials'" not in app:
              app = app.replace(
                  "  rules: ['Monitoring Rules', 'Thresholds used by services and problems'],",
                  "  credentials: ['Credentials', 'Encrypted WinRM, SSH and SNMP credential profiles'],\n  rules: ['Monitoring Rules', 'Thresholds used by services and problems'],"
              )
          app = app.replace('Latest agent telemetry', 'Latest telemetry')
          if 'async function renderCredentials()' not in app:
              ui = r'''
async function renderCredentials() {
  const rows=await api('/api/v1/credentials');
  content.innerHTML=`<div class="page-stack"><div class="grid-2"><article class="panel"><div class="panel-head"><div><h2>Add credential profile</h2><p>Secrets are encrypted before they are stored.</p></div></div><div class="panel-body"><form id="credential-form" class="form-stack"><div class="form-grid"><label>Name<input id="cred-name" required placeholder="Windows monitoring"/></label><label>Type<select id="cred-type"><option value="winrm">Windows / WinRM</option><option value="ssh_password">Linux / SSH password</option><option value="ssh_key">Linux / SSH private key</option><option value="snmp_v2">SNMP v2c</option><option value="snmp_v3">SNMP v3</option></select></label><label>Username<input id="cred-user" placeholder="monitoring-user"/></label><label>Secret / password / community<textarea id="cred-secret" required></textarea></label><label>Secondary secret<textarea id="cred-secret2" placeholder="SNMPv3 privacy password (optional)"></textarea></label><label>Options JSON<textarea id="cred-options">{}</textarea></label></div><button class="button primary" type="submit">Save encrypted credential</button></form></div></article><article class="panel"><div class="panel-head"><div><h2>Security</h2><p>Credential handling</p></div></div><div class="panel-body"><div class="notice">Passwords, communities and private keys are encrypted using <code>SENTINEL_CREDENTIAL_SECRET</code>. The API never returns plaintext secrets. Change the development default before production use.</div></div></article></div><article class="panel"><div class="panel-head"><div><h2>Credential profiles</h2><p>${rows.length} configured</p></div></div><div class="table-wrap"><table><thead><tr><th>Name</th><th>Type</th><th>Username</th><th>Secret</th><th>Updated</th><th></th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${esc(r.name)}</strong></td><td>${esc(r.credential_type)}</td><td>${esc(r.username||'—')}</td><td>${r.has_secret?badge('up','encrypted'):badge('unknown','none')}</td><td>${fmtDate(r.updated_at)}</td><td><button class="link-button danger" data-delete-credential="${esc(r.id)}">Delete</button></td></tr>`).join('')||`<tr><td colspan="6">${empty('No credential profiles yet.')}</td></tr>`}</tbody></table></div></article></div>`;
  $('#credential-form').onsubmit=async event=>{event.preventDefault();try{let options={};try{options=JSON.parse($('#cred-options').value||'{}');}catch{throw new Error('Options must be valid JSON');}await api('/api/v1/credentials',{method:'POST',body:JSON.stringify({name:$('#cred-name').value.trim(),credential_type:$('#cred-type').value,username:$('#cred-user').value.trim()||null,secret:$('#cred-secret').value,secondary_secret:$('#cred-secret2').value||null,options})});toast('Credential profile saved');await renderCredentials();}catch(e){toast(e.message,true);}};
  $$('[data-delete-credential]').forEach(btn=>btn.onclick=async()=>{if(!confirm('Delete this credential profile?'))return;try{await api(`/api/v1/credentials/${btn.dataset.deleteCredential}`,{method:'DELETE'});toast('Credential deleted');await renderCredentials();}catch(e){toast(e.message,true);}});
}

async function renderAgentless() {
  const [credentials,monitors]=await Promise.all([api('/api/v1/credentials'),api('/api/v1/agentless/monitors')]);
  let candidates=[];
  const credentialOptions=credentials.map(c=>`<option value="${esc(c.id)}">${esc(c.name)} · ${esc(c.credential_type)}</option>`).join('');
  content.innerHTML=`<div class="page-stack"><div class="grid-main-aside"><article class="panel"><div class="panel-head"><div><h2>Agentless monitoring</h2><p>Use a whole CIDR, discovered hosts only, or select individual IP addresses.</p></div></div><div class="panel-body"><form id="agentless-form" class="form-stack"><div class="form-grid"><label>Network / CIDR<input id="al-cidr" value="192.168.1.0/24" required/></label><label>Scope<select id="al-scope"><option value="selected">Selected IPs</option><option value="discovered">Discovered hosts in CIDR</option><option value="cidr_all">Entire CIDR</option></select></label><label>Method<select id="al-method"><option value="winrm">WinRM (Windows)</option><option value="ssh">SSH (Linux/Unix)</option><option value="snmp">SNMP</option></select></label><label>Credential<select id="al-credential" required>${credentialOptions}</select></label><label>Poll interval (seconds)<input id="al-interval" type="number" min="15" max="3600" value="60"/></label><label>Site<input id="al-site" value="default"/></label></div><div class="panel-actions"><button id="load-range" class="button secondary" type="button">List CIDR</button><button id="test-agentless" class="button secondary" type="button">Test first selected</button><button class="button primary" type="submit">Enable monitoring</button></div></form></div></article><article class="panel"><div class="panel-head"><div><h2>Mode</h2><p>Agent is optional</p></div></div><div class="panel-body"><div class="notice"><strong>WinRM</strong> collects Windows CPU/RAM/disk without an agent. <strong>SSH</strong> collects Linux performance. <strong>SNMP</strong> covers switches, routers, UPS and appliances. Sentinel Agent remains available for richer/high-frequency telemetry.</div></div></article></div><article class="panel"><div class="panel-head"><div><h2>CIDR candidates</h2><p id="candidate-caption">Load a CIDR to list every address.</p></div><div class="panel-actions"><button id="select-all-candidates" class="button ghost small" type="button">Select all</button><button id="clear-candidates" class="button ghost small" type="button">Clear</button></div></div><div class="table-wrap"><table><thead><tr><th></th><th>IP</th><th>Hostname</th><th>Inventory</th><th>State</th></tr></thead><tbody id="candidate-body"><tr><td colspan="5">${empty('No range loaded.')}</td></tr></tbody></table></div></article><article class="panel"><div class="panel-head"><div><h2>Agentless monitors</h2><p>${monitors.length} configured</p></div></div><div class="table-wrap"><table><thead><tr><th>Status</th><th>Host</th><th>Method</th><th>Credential</th><th>CPU</th><th>Memory</th><th>Last success</th><th>Error</th><th></th></tr></thead><tbody>${monitors.map(m=>`<tr><td>${badge(!m.enabled?'unknown':m.last_error&&m.consecutive_failures>=3?'critical':m.last_success_at?'online':'pending')}</td><td><div class="primary-cell"><strong>${esc(m.hostname||m.ip_address||'Unknown')}</strong><span>${esc(m.ip_address||'')}</span></div></td><td>${esc(m.method)}</td><td>${esc(m.credential_name||'—')}</td><td>${m.telemetry?.cpu_usage_percent==null?'—':metricValue(m.telemetry.cpu_usage_percent,'%')}</td><td>${m.telemetry?.memory_usage_percent==null?'—':metricValue(m.telemetry.memory_usage_percent,'%')}</td><td>${ago(m.last_success_at)}</td><td>${esc(m.last_error||'—')}</td><td><button class="link-button" data-poll-monitor="${esc(m.id)}">Poll</button> <button class="link-button danger" data-delete-monitor="${esc(m.id)}">Delete</button></td></tr>`).join('')||`<tr><td colspan="9">${empty('No agentless monitors configured.')}</td></tr>`}</tbody></table></div></article></div>`;
  const drawCandidates=()=>{$('#candidate-caption').textContent=`${candidates.length} usable addresses · ${candidates.filter(c=>c.discovered).length} already discovered`;$('#candidate-body').innerHTML=candidates.map(c=>`<tr><td><input class="candidate-check" type="checkbox" data-target="${esc(c.ip_address)}" ${c.discovered?'checked':''}/></td><td class="mono">${esc(c.ip_address)}</td><td>${esc(c.hostname||'—')}</td><td>${c.discovered?badge('up','discovered'):badge('unknown','unseen')}</td><td>${badge(c.state)}</td></tr>`).join('')||`<tr><td colspan="5">${empty('No candidates.')}</td></tr>`;};
  $('#load-range').onclick=async()=>{try{candidates=await api(`/api/v1/agentless/candidates${qs({cidr:$('#al-cidr').value.trim()})}`);drawCandidates();}catch(e){toast(e.message,true);}};
  $('#select-all-candidates').onclick=()=>$$('.candidate-check').forEach(x=>x.checked=true);$('#clear-candidates').onclick=()=>$$('.candidate-check').forEach(x=>x.checked=false);
  const selectedTargets=()=>$$('.candidate-check').filter(x=>x.checked).map(x=>x.dataset.target);
  $('#test-agentless').onclick=async()=>{const target=selectedTargets()[0];if(!target){toast('Select at least one IP first',true);return;}try{const r=await api('/api/v1/agentless/test',{method:'POST',body:JSON.stringify({method:$('#al-method').value,credential_id:$('#al-credential').value,target})});toast(`Connected to ${r.sample.hostname||target}`);}catch(e){toast(e.message,true);}};
  $('#agentless-form').onsubmit=async event=>{event.preventDefault();try{const scope=$('#al-scope').value;const payload={method:$('#al-method').value,credential_id:$('#al-credential').value,scope,cidr:scope==='selected'?null:$('#al-cidr').value.trim(),targets:scope==='selected'?selectedTargets():[],site:$('#al-site').value.trim()||null,interval_seconds:Number($('#al-interval').value)};const result=await api('/api/v1/agentless/monitors/bulk',{method:'POST',body:JSON.stringify(payload)});toast(`Monitoring enabled for ${result.targets} target(s)`);await renderAgentless();}catch(e){toast(e.message,true);}};
  $$('[data-poll-monitor]').forEach(btn=>btn.onclick=async()=>{try{await api(`/api/v1/agentless/monitors/${btn.dataset.pollMonitor}/poll`,{method:'POST'});toast('Immediate poll queued');}catch(e){toast(e.message,true);}});
  $$('[data-delete-monitor]').forEach(btn=>btn.onclick=async()=>{if(!confirm('Delete this monitor?'))return;try{await api(`/api/v1/agentless/monitors/${btn.dataset.deleteMonitor}`,{method:'DELETE'});toast('Monitor deleted');await renderAgentless();}catch(e){toast(e.message,true);}});
}

'''
              app = app.replace('async function renderSettings() {', ui + 'async function renderSettings() {', 1)
          app = app.replace(
              'const renderers={overview:renderOverview,hosts:renderHosts,host:renderHost,services:renderServices,problems:renderProblems,events:renderEvents,topology:renderTopology,discovery:renderDiscovery,devices:renderDevices,agents:renderAgents,policies:renderPolicies,rules:renderRules,snmp:renderSnmp,integrations:renderIntegrations,notifications:renderNotifications,maintenance:renderMaintenance,availability:renderAvailability,users:renderUsers,audit:renderAudit,settings:renderSettings};',
              'const renderers={overview:renderOverview,hosts:renderHosts,host:renderHost,services:renderServices,problems:renderProblems,events:renderEvents,topology:renderTopology,discovery:renderDiscovery,devices:renderDevices,agents:renderAgents,agentless:renderAgentless,policies:renderPolicies,credentials:renderCredentials,rules:renderRules,snmp:renderSnmp,integrations:renderIntegrations,notifications:renderNotifications,maintenance:renderMaintenance,availability:renderAvailability,users:renderUsers,audit:renderAudit,settings:renderSettings};'
          )
          app = app.replace(
              '<button class="button primary" data-quick-nav="discovery">Discover network</button><button class="button secondary" data-quick-nav="agents">Install managed agent</button>',
              '<button class="button primary" data-quick-nav="discovery">Discover network</button><button class="button secondary" data-quick-nav="agentless">Configure agentless monitoring</button><button class="button secondary" data-quick-nav="agents">Install managed agent</button>'
          )
          write('services/web-ui/js/app.js', app)

          tests = r'''import uuid
from fastapi.testclient import TestClient
from app.main import app

client = TestClient(app)
ADMIN = {"X-API-Key": "change-me-now"}


def test_agentless_credentials_and_cidr_modes():
    suffix = uuid.uuid4().hex[:8]
    name = f"agentless-{suffix}"
    created = client.post("/api/v1/credentials", headers=ADMIN, json={
        "name": name, "credential_type": "winrm", "username": "monitor",
        "secret": "not-a-real-password", "options": {"transport": "ntlm"},
    })
    assert created.status_code == 200, created.text
    credential = created.json()
    assert credential["has_secret"] is True
    assert "secret" not in credential and "secret_encrypted" not in credential

    rows = client.get("/api/v1/credentials", headers=ADMIN)
    assert rows.status_code == 200
    row = next(x for x in rows.json() if x["id"] == credential["id"])
    assert "secret" not in row and "secret_encrypted" not in row

    candidates = client.get("/api/v1/agentless/candidates?cidr=192.168.248.0/30", headers=ADMIN)
    assert candidates.status_code == 200, candidates.text
    assert [x["ip_address"] for x in candidates.json()] == ["192.168.248.1", "192.168.248.2"]

    bulk = client.post("/api/v1/agentless/monitors/bulk", headers=ADMIN, json={
        "method": "winrm", "credential_id": credential["id"], "scope": "cidr_all",
        "cidr": "192.168.248.0/30", "site": "agentless-test", "interval_seconds": 60,
    })
    assert bulk.status_code == 200, bulk.text
    assert bulk.json()["targets"] == 2

    monitors = client.get("/api/v1/agentless/monitors", headers=ADMIN)
    assert monitors.status_code == 200
    ours = [m for m in monitors.json() if m["credential_id"] == credential["id"]]
    assert len(ours) == 2


def test_agentless_selected_scope_rejects_empty_selection():
    suffix = uuid.uuid4().hex[:8]
    cred = client.post("/api/v1/credentials", headers=ADMIN, json={
        "name": f"ssh-{suffix}", "credential_type": "ssh_password", "username": "monitor",
        "secret": "not-a-real-password", "options": {},
    }).json()
    response = client.post("/api/v1/agentless/monitors/bulk", headers=ADMIN, json={
        "method": "ssh", "credential_id": cred["id"], "scope": "selected", "targets": [],
    })
    assert response.status_code == 400
'''
          write('services/control-api/tests/test_agentless.py', tests)

          docs = r'''# Agentless monitoring

SentinelView supports agentless monitoring as a first-class alternative to the managed Sentinel Agent.

## Collection methods

- **WinRM**: Windows CPU, memory, disks, uptime, process count and network counters through remote PowerShell/CIM.
- **SSH**: Linux/Unix CPU, memory, disks, uptime, process count and `/proc/net/dev` counters.
- **SNMP**: generic availability, sysName, sysUpTime and HOST-RESOURCES CPU polling. Prometheus `snmp_exporter` remains available for richer vendor-specific SNMP modules.
- **Sentinel Agent**: optional enhanced collection for higher-frequency and deeper endpoint telemetry.

## CIDR workflow

The Agentless page supports three scopes:

1. `Selected IPs` — load a range such as `192.168.1.0/24`, then tick only the hosts you want.
2. `Discovered hosts in CIDR` — apply a credential/method only to inventory entries already found by Discovery.
3. `Entire CIDR` — create monitoring assignments for every usable address in the authorized private CIDR.

Public ranges remain rejected by the same private-network guard used by Discovery.

## Credentials

Credential profiles are stored in `credential_profiles`. Passwords, SNMP communities and private keys are serialized and encrypted with Fernet using a key derived from `SENTINEL_CREDENTIAL_SECRET`. API responses never expose the ciphertext or plaintext secret.

Set a strong stable value before production use:

```text
SENTINEL_CREDENTIAL_SECRET=<random-long-secret>
```

Changing this value after credentials have been created makes existing credential payloads undecryptable, so rotate credentials deliberately rather than changing the key casually.

## Container worker

`agentless-worker` is a separate Docker service. It polls enabled assignments from PostgreSQL and executes collector jobs concurrently. Metrics are written to `agentless_telemetry_latest`; the existing Control API `/metrics` endpoint republishes them as SentinelView Prometheus metrics so the current host graphs continue to work.

The worker marks a target down only after three consecutive collection failures to reduce transient flapping.

## Windows prerequisites

The Windows target must permit WinRM from the SentinelView collector network and the supplied account must have permission to query the requested CIM/performance data. SentinelView does not automatically weaken target-side WinRM policy.

## SSH host keys

Strict host-key checking can be enabled through credential options with `{"strict_host_key": true}`. The default lab-friendly behavior accepts an unknown host key; production deployments should enable strict verification and provide trusted host keys in the collector image/environment.
'''
          write('docs/AGENTLESS_MONITORING.md', docs)

      - name: Python compile
        run: python -m compileall -q services/control-api/app services/control-api/tests

      - name: Install backend dependencies and run tests
        run: |
          pip install -r services/control-api/requirements.txt
          PYTHONPATH=services/control-api SENTINEL_DB_URL=sqlite:////tmp/sentinel-agentless-ci.db SENTINEL_CREDENTIAL_SECRET=ci-agentless-secret pytest -q services/control-api/tests

      - name: Validate UI and config
        run: |
          npm --version
          node --check services/web-ui/js/api.js
          node --check services/web-ui/js/ui.js
          node --check services/web-ui/js/app.js
          pip install pyyaml
          python scripts/validate-config.py

      - name: Remove one-shot workflow and commit feature
        run: |
          rm .github/workflows/one-shot-agentless-monitoring.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'feat(agentless): add WinRM SSH SNMP monitoring and credential manager'
          git push origin HEAD:feature/agentless-monitoring-ci
